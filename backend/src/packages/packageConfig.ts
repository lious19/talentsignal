/**
 * PROPOSED — pending Ali's approval. See 06_decisions/016. S-09's build note
 * doesn't say how many candidates a drafted package should carry; this is the
 * "small judgment call — propose it, log it" the story explicitly invites.
 */
export const PACKAGE_CONFIG = {
  topCandidateCount: 3,
};
